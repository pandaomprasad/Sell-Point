
import { View, Text, StyleSheet } from 'react-native';

const Gym_Details = () => {
  return (
    <View style={styles.container}>
      <Text>Gym_Details Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default Gym_Details;
