
import { View, Text, StyleSheet } from 'react-native';

const Job_Details = () => {
  return (
    <View style={styles.container}>
      <Text>Job_Details Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default Job_Details;
